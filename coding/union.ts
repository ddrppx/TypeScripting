let scoreboard: number | string;

scoreboard = 20;
scoreboard = '6 x 0';

const array_data: ( number | boolean )[] = [1, 3, true, 4, 5, false];
const carType: ("sedan" | "hatch"| "truck") = "sedan";

