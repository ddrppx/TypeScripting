// Strings
let greeting: string = "Oi Pedro"

// Number ( int, float, etc)
let number: number = 1123

// boolean
let isLoggedIn: boolean = false

// Obvious variable types arent needed, when blatantly assigned value, typescript will asign its type on its own.

let test_var = 123;
test_var = "123"; // ! Type string not assignable to type number

console.log(greeting);
console.log("Num:", number);
